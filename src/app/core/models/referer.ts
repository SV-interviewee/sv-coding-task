export interface Referer {
  referer_medium: string;
  referer_url?: string;
  referer_source?: string;
  referer_term?: string;
  referer_device?: string;
  referer_network?: string;
  referer_content?: string;
  referer_campaign?: string;
}
